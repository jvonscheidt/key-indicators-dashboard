# Market Indicators Dashboard — pinned direct dependencies.
# Install with:  .venv/bin/pip install -r requirements.txt
# Transitive deps are resolved by pip; only direct imports are pinned here.

# UI / charting
streamlit==1.58.0
plotly==6.8.0

# Data sources
yfinance==1.5.1
fredapi==0.5.2
requests==2.34.2
beautifulsoup4==4.15.0

# Core data handling
pandas==3.0.3

# Resilience
tenacity==9.1.4

# Tests / formatting / linting
black==26.5.1
pytest==9.0.3
ruff==0.15.16
