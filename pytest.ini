[pytest]
# `pytest` (the bare console script, as used in CI and README) does not put
# the repo root on sys.path the way `python -m pytest` does, so `import app`
# and `from data import ...` in tests/ fail without this.
pythonpath = .
