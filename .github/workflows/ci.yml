name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python version
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip

      # Install from requirements.txt so Black, Ruff, and pytest match the
      # pinned versions developers run locally.
      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Check formatting
        run: black --check .

      - name: Lint
        run: ruff check .

      - name: Run tests
        run: pytest -q
